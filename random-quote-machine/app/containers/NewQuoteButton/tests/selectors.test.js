import {
  selectNewQuoteButton,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectNewQuoteButton();

describe('selectNewQuoteButton', () => {

});
